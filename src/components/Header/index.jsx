import React from 'react';

import { Cart } from 'components';
import { HeaderWrapper } from './styles';

export const Header = () => {
  return (
    <HeaderWrapper>
      <Cart />
    </HeaderWrapper>
  );
};
